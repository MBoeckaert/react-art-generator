// import PropTypes from "prop-types";

const Generator = () => {
  return (
    <p>Here comes the art Generator</p>
  );
};

// Inputs.propTypes = {
//   item: PropTypes.object.isRequired,
// };

export default Generator;