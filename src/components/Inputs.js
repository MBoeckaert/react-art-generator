// import PropTypes from "prop-types";

const Inputs = () => {
  return (
    <p>Here comes the form inputs</p>
  );
};

// Inputs.propTypes = {
//   item: PropTypes.object.isRequired,
// };

export default Inputs;